import Timer from './Timer'

const Header = () => {
    return ( 
        <div className="header">
            <button>Affiché le profile</button>
            <Timer />
        </div>
    );
}

export default Header;